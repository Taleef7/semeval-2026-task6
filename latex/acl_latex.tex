\documentclass[11pt]{article}

% Change "review" to "final" to generate the final (sometimes called camera-ready) version.
% Change to "preprint" to generate a non-anonymous version with page numbers.
\usepackage[final]{acl}  % SemEval: non-anonymous submission

% Standard package includes
\usepackage{times}
\usepackage{latexsym}

% For proper rendering and hyphenation of words containing Latin characters (including in bib files)
\usepackage[T1]{fontenc}

% This assumes your files are encoded as UTF8
\usepackage[utf8]{inputenc}

% This is not strictly necessary, and may be commented out,
% but it will improve the layout of the manuscript,
% and will typically save some space.
\usepackage{microtype}

% This is also not strictly necessary, and may be commented out.
% However, it will improve the aesthetics of text in
% the typewriter font.
\usepackage{inconsolata}

% Including images in your LaTeX document requires adding
% additional package(s)
\usepackage{graphicx}
\graphicspath{{../docs/paper_figures/}}

% Additional useful packages
\usepackage{float}
\usepackage{url}         % Safe URL handling
\usepackage{booktabs}    % Professional-quality tables
\usepackage{multirow}    % Multi-row cells in tables
\usepackage{amsmath}     % Math
\usepackage{xcolor}      % Colors for annotations
\usepackage{tikz}        % Architecture diagram
\usetikzlibrary{positioning, arrows.meta, fit, backgrounds}

\title{PFW at SemEval-2026 Task 6: Multi-Seed DeBERTa Ensembles for Political Response Clarity and Evasion Classification}

\author{Taleef Tamsal\thanks{Our official submissions appear under the participant name ``taleef'' on the Codabench and task page leaderboards.} \and Jonathan Rusert \\
  Purdue University Fort Wayne \\
  \texttt{\{tamst01, jrusert\}@pfw.edu}}

\begin{document}
\maketitle

% ============================================================
% ABSTRACT
% ============================================================
\begin{abstract}
This paper describes the PFW system for SemEval-2026 Task~6 (CLARITY), which addresses the classification of response clarity and evasion techniques in political interview question-answer pairs.
Rather than relying on large language model prompting, we pursue a competitive non-LLM approach based on fine-tuning DeBERTa-xlarge and DeBERTa-v3-large with a multi-seed ensemble strategy: 5-fold cross-validation with 10 random seeds yields 50 models per architecture, combined through simple logit averaging.
Our system achieves a macro F1 of 0.76 on Subtask~1 (clarity-level classification) and 0.50 on Subtask~2 (evasion-type classification).
We also find that three post-hoc optimization techniques---learned ensemble weights, threshold calibration, and hierarchical masking---each improve out-of-fold performance yet \emph{degrade} evaluation scores by 0.02--0.10 F1. This pattern should be interpreted cautiously: the 237-sample evaluation set likely contributes substantial variance, and two of the three degradations fall within the $\pm$0.06 95\% CI expected from sampling noise. Still, the consistent directional pattern across all three prediction-level interventions provides suggestive evidence for an optimization paradox, highlighting the risk of overfitting to cross-validation predictions when evaluation data is limited.
Our code is publicly available at \url{https://github.com/Taleef7/semeval-2026-task6}.
\end{abstract}

% ============================================================
% 1. INTRODUCTION
% ============================================================
\section{Introduction}

Political question evasion, the practice of providing answers that deflect, dodge, or otherwise fail to address the substance of a question, is a pervasive phenomenon in political discourse and has been studied extensively in political science and communication research \citep{bull2003microanalysis, clayman2001answers, harris1991evasive}. Automatically detecting and classifying such evasion is important for media accountability and computational political science, yet it remains challenging because evasive language is often subtle and highly context-dependent.

SemEval-2026 Task~6, CLARITY \citep{semeval2026task6}, formalizes this problem as two classification tasks over the QEvasion dataset \citep{thomas2024clarity}. \textbf{Subtask~1} predicts one of three clarity levels (\textit{Clear Reply}, \textit{Ambivalent}, \textit{Clear Non-Reply}), while \textbf{Subtask~2} predicts one of nine fine-grained evasion types. The taxonomy matters because each evasion type belongs to exactly one clarity level, so systems can either exploit or ignore meaningful hierarchical structure. Both subtasks are evaluated with macro F1, which gives equal weight to each class and therefore makes severe imbalance especially consequential (13$\times$ ratio between the largest and smallest classes in Subtask~2).

Our approach deliberately avoids LLM-based prompting strategies and instead asks how far \textbf{smaller, fine-tuned encoder models} can go with careful ensembling. We train 50 models per architecture (DeBERTa-xlarge, 900M; DeBERTa-v3-large, 304M) using different random seeds and aggregate them with simple logit averaging. This design philosophy prioritizes \textbf{accessibility and reproducibility}: our largest model is under 1B parameters, runs on a single GPU, and requires no API access or proprietary LLM inference, making the system practical for researchers with limited computational budgets. The multi-seed strategy is motivated by the well-documented instability of fine-tuning large pre-trained language models \citep{dodge2020finetuning}, where individual runs can vary by up to 0.10 F1 from random seed differences alone.

Our system achieves macro F1 of \textbf{0.76} (Subtask~1) and \textbf{0.50} (Subtask~2). Beyond these results, the main scientific contribution of this paper is what we term the \textbf{optimization paradox}: three independent post-hoc calibration techniques, each tuned on out-of-fold (OOF) predictions, consistently improved OOF metrics but degraded evaluation scores by 0.02--0.10 F1. We frame this as suggestive rather than definitive evidence. With only 237 evaluation samples, two of the three degradations lie within the $\pm$0.06 CI expected from sampling noise, so the observation is interesting primarily because of the \emph{consistent directional pattern} across interventions. We interpret the pattern as a distinction between \textit{model-level} interventions (multi-seed ensembling, which transfers) and \textit{prediction-level} interventions (post-hoc calibration, which appears more prone to overfitting), offering a practical guideline for shared-task settings with limited evaluation data. Our error analysis further shows that Subtask~2 difficulty is driven by semantic overlap between evasion categories and extreme class imbalance, with four minority classes achieving F1 below 0.27.

% ============================================================
% 2. BACKGROUND
% ============================================================
\section{Background}

\subsection{Task and Dataset}

The CLARITY shared task follows the two-level taxonomy from \citet{thomas2024clarity}. At the coarse level, responses are categorized as \textit{Clear Reply}, \textit{Ambivalent}, or \textit{Clear Non-Reply}. At the fine-grained level, nine evasion types (e.g., \textit{Dodging}, \textit{Deflection}, \textit{Implicit}) each map to exactly one clarity level, making the taxonomy both linguistically meaningful and operationally useful for modeling. The QEvasion dataset \citep{thomas2024clarity} contains 3,448 English question-answer pairs from political interviews, annotated using a combination of GPT-3.5 and human annotators. The held-out SemEval evaluation set contains 237 samples. Table~\ref{tab:class_dist} shows the training distribution: \textit{Ambivalent} dominates Subtask~1 (59.2\%), while Subtask~2 is severely long-tailed, with \textit{Partial/half-answer} comprising only 2.3\% of samples.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}llrr@{}}
\toprule
\textbf{Clarity Level} & \textbf{Evasion Type} & \textbf{N} & \textbf{\%} \\
\midrule
Clear Reply & Explicit & 1,052 & 30.5 \\
\midrule
\multirow{5}{*}{Ambivalent} & Dodging & 706 & 20.5 \\
 & Implicit & 488 & 14.2 \\
 & General & 386 & 11.2 \\
 & Deflection & 381 & 11.0 \\
 & Partial/half-answer & 79 & 2.3 \\
\midrule
\multirow{3}{*}{Clear Non-Reply} & Declining to answer & 145 & 4.2 \\
 & Claims ignorance & 119 & 3.5 \\
 & Clarification & 92 & 2.7 \\
\bottomrule
\end{tabular}
\caption{Training set class distribution (N=3,448) with hierarchical label structure. Five of nine evasion types comprise $<$5\% of samples.}
\label{tab:class_dist}
\end{table}

\subsection{Related Work}

Political evasion has been studied through qualitative frameworks \citep{bull2003microanalysis, harris1991evasive, clayman2001answers}. \citet{thomas2024clarity} bridged this with NLP, introducing QEvasion with encoder (DeBERTa/RoBERTa/XLNet) and LLM (ChatGPT, Llama, Falcon) baselines. Our work builds on the DeBERTa family \citep{he2021deberta, he2023debertav3}. The multi-seed ensemble approach is motivated by findings that fine-tuning pre-trained Transformers is highly sensitive to random initialization \citep{dodge2020finetuning, mosbach2021stability} and by the deep-ensembles result that averaging independently trained networks yields robust gains in accuracy and calibration \citep{lakshminarayanan2017ensembles}. Our contribution is empirical: we document the scale of seed-induced variance on a pragmatic classification task (up to 0.10 F1 spread) and characterize the OOF-vs-evaluation behavior of seed ensembling versus post-hoc calibration on a small, imbalanced evaluation set, where such calibration is known to be sensitive to validation composition \citep{guo2017calibration}.

% ============================================================
% 3. SYSTEM OVERVIEW
% ============================================================
\section{System Overview}

\subsection{Design Philosophy}

We chose fine-tuned encoder models over LLM prompting for three reasons. First, \textbf{parameter efficiency}: our largest model (DeBERTa-xlarge, 900M parameters) is 7--70$\times$ smaller than typical prompted LLMs (7B--70B). Second, \textbf{reproducibility}: fine-tuning with fixed seeds is fully deterministic, unlike temperature-dependent LLM generation. Third, \textbf{task adaptation}: fine-tuning on the full training set lets the model absorb task-specific annotation conventions that are difficult to communicate reliably through prompts alone.

To validate this choice, we evaluated Qwen2.5-32B-Instruct \citep{yang2024qwen25} in a 5-shot prompting setup (per-class exemplars, greedy decoding, temperature~0) on the training set (N=3,448). Table~\ref{tab:llm_bias} shows the resulting label distribution. The naively prompted LLM exhibits severe label bias: it massively over-predicts \textit{Dodging} and \textit{Partial/half-answer} while nearly ignoring the most frequent class, \textit{Explicit}. We emphasize that this is a test of \emph{naive 5-shot prompting without chain-of-thought (CoT) or hierarchical decomposition} on a single model, not a claim about LLMs in general. It supports the narrower conclusion that encoder fine-tuning is a strong, simple baseline relative to naively prompted mid-scale LLMs. Consistent with the official task overview \citep{semeval2026task6}, stronger shared-task systems used more capable LLM prompting pipelines and hierarchical exploitation of the taxonomy, and we do not dispute that such systems can surpass encoder ensembles on CLARITY.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Evasion Type} & \textbf{True \%} & \textbf{Pred.~\%} \\
\midrule
Explicit & 30.5 & 4.5 \\
Dodging & 20.5 & 48.7 \\
Implicit & 14.2 & 1.6 \\
General & 11.2 & 4.9 \\
Partial/half-answer & 2.3 & 34.7 \\
\textit{4 others} & 21.3 & 5.6 \\
\bottomrule
\end{tabular}
\caption{Label distribution: Qwen2.5-32B 5-shot predictions vs.\ true labels. The LLM massively over-predicts \textit{Dodging} and \textit{Partial/half-answer} while ignoring \textit{Explicit}.}
\label{tab:llm_bias}
\end{table}

\subsection{Model Architecture}

We use \textbf{DeBERTa-xlarge} (900M params, disentangled attention) for Subtask~1 and \textbf{DeBERTa-v3-large} (304M params, ELECTRA-style pre-training) for Subtask~2. Both use CLS pooling $\rightarrow$ dropout ($p{=}0.1$) $\rightarrow$ linear classifier. Input format: \texttt{Question: \{q\}\textbackslash nAnswer: \{a\}}, max 512 tokens. Figure~\ref{fig:architecture} illustrates the system architecture.

\begin{figure}[t]
\centering
\small
\begin{tikzpicture}[
    node distance=0.4cm,
    box/.style={draw, rounded corners, minimum width=2.8cm, minimum height=0.55cm, align=center, font=\footnotesize},
    smallbox/.style={draw, rounded corners, minimum width=1.8cm, minimum height=0.45cm, align=center, font=\scriptsize},
    arr/.style={-{Stealth[length=2mm]}, thick},
    dasharr/.style={-{Stealth[length=2mm]}, thick, dashed},
]
% Single model pipeline
\node[box, fill=blue!8] (input) {\texttt{Q: \{q\}$\backslash$nA: \{a\}}};
\node[box, fill=blue!15, below=of input] (encoder) {DeBERTa Encoder};
\node[box, fill=blue!10, below=of encoder] (cls) {[CLS] Pooling};
\node[box, fill=orange!12, below=of cls] (head) {Dropout $\rightarrow$ Linear};
\node[box, fill=orange!18, below=of head] (logits) {Logits $\mathbf{z}_m$};

\draw[arr] (input) -- (encoder);
\draw[arr] (encoder) -- (cls);
\draw[arr] (cls) -- (head);
\draw[arr] (head) -- (logits);

% Ensemble wrapper
\node[below=0.5cm of logits, font=\footnotesize, align=center] (ens_label) {$\times\, M{=}50$ models\\[-2pt]{\scriptsize(5 folds $\times$ 10 seeds)}};
\node[box, fill=green!15, below=0.4cm of ens_label] (avg) {Logit Averaging (Eq.~\ref{eq:ensemble})};
\node[box, fill=green!22, below=of avg] (pred) {Prediction $\hat{y}$};

\draw[dasharr] (logits) -- (ens_label);
\draw[arr] (ens_label) -- (avg);
\draw[arr] (avg) -- (pred);

% Brace for single model
\begin{scope}[on background layer]
\node[draw=gray, dashed, rounded corners, fit=(input)(logits), inner sep=4pt, label={[font=\scriptsize, gray]left:Single model}] {};
\end{scope}
\end{tikzpicture}
\caption{System architecture. Each model independently maps a QA pair through a DeBERTa encoder with a linear classification head. Final predictions aggregate logits from 50 models via simple averaging.}
\label{fig:architecture}
\end{figure}

\subsection{Multi-Seed Ensemble Strategy}
\label{sec:ensemble}

We train 50 models per architecture via 5-fold stratified cross-validation $\times$ 10 random seeds. At inference, we average raw logits across all models:
\begin{equation}
\hat{y} = \arg\max_c \frac{1}{M} \sum_{m=1}^{M} z_m^{(c)}
\label{eq:ensemble}
\end{equation}
where $z_m^{(c)}$ is the logit for class $c$ from model $m$ and $M{=}50$. This is motivated by the high variance across seeds: in our DeBERTa-v3-large baseline experiments on Subtask~1 (before selecting xlarge as the final T1 architecture), individual models ranged from 0.601 to 0.698 macro F1 ($\sigma{=}0.024$), which is a spread of nearly 0.10 purely from random initialization.

Subtask~1 and~2 predictions are made \textbf{independently}: we do not condition Subtask~2 on Subtask~1 outputs, as hierarchical conditioning amplified cascading errors (Section~\ref{sec:paradox}).

% ============================================================
% 4. EXPERIMENTAL SETUP
% ============================================================
\section{Experimental Setup}

All models share identical hyperparameters: learning rate $2{\times}10^{-5}$ with 10\% linear warmup, AdamW \citep{loshchilov2019adamw} with weight decay 0.01, effective batch size 32 (4 $\times$ 8 gradient accumulation), cross-entropy loss, 3 epochs, using HuggingFace Transformers \citep{wolf2020transformers}. Training was performed on NVIDIA A100-80GB GPUs (university HPC cluster). Each xlarge model takes $\sim$45~min and v3-large $\sim$15~min, totaling $\sim$50 GPU-hours for all 100 models. Full hyperparameters are listed in Appendix~\ref{sec:hyperparams}.

% ============================================================
% 5. RESULTS AND ANALYSIS
% ============================================================
\section{Results and Analysis}

\subsection{Official Results and Baselines}

Table~\ref{tab:results} presents our development baselines, ensemble results, and official evaluation scores. The official task overview reports that our system achieved macro F1~=~0.76 on Subtask~1 and 0.50 on Subtask~2, while the top system reached 0.89 and 0.68 respectively \citep{semeval2026task6}. These scores correspond to \textbf{18th of 41 participants} on Subtask~1 and \textbf{12th of 33 participants} on Subtask~2 on the official CLARITY task page.\footnote{Rank placement taken from the official CLARITY task page: \url{https://konstantinosftw.github.io/CLARITY-SemEval-2026/}} This gap is consistent with the official task paper's broader finding that the strongest systems generally relied on LLM prompting and hierarchical exploitation of the taxonomy, whereas systems treating the two subtasks independently were generally weaker \citep{semeval2026task6}. Our approach intentionally takes the latter path: two independently fine-tuned encoder ensembles, chosen for accessibility, sub-1B scale, single-GPU training, and no API dependence.

For prior work, Table~\ref{tab:results} compares against the original QEvasion baselines from \citet{thomas2024clarity}. That paper does not report a plain BERT baseline, so the closest encoder references are DeBERTa-base, RoBERTa-base, and XLNet-base, alongside ChatGPT as the reported LLM baseline.

The 50-model DeBERTa-v3-large ensemble improves over the single-model mean by +0.024 on Subtask~1 OOF (0.667 vs.\ 0.643) and +0.046 on Subtask~2 OOF (0.373 vs.\ 0.327). For DeBERTa-xlarge on Subtask~1, our final submission architecture, the 50-model ensemble improves from 0.663~$\pm$~0.021 to 0.679 (+0.016 OOF).

The higher evaluation F1 relative to OOF (T1: 0.76 vs.\ 0.679, +0.081; T2: 0.50 vs.\ 0.373, +0.127) reflects a structural difference---OOF models see only $4/5$ of the training data, whereas evaluation predictions aggregate 50 models trained on the full set---compounded on T2 by macro F1's extreme sensitivity under severe imbalance, where a handful of minority-class predictions among 237 samples can swing the score by multiple points. We cannot fully rule out that part of either jump reflects favorable minority-class sampling in the evaluation set, and both numbers should be read with this caveat.

\begin{table}[t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{System} & \textbf{T1} & \textbf{T2} \\
\midrule
\multicolumn{3}{l}{\textit{Prior work baselines$^\dagger$}} \\
\quad ChatGPT (ZS) \citep{thomas2024clarity} & 0.413 & 0.244 \\
\quad DeBERTa-base (FT) \citep{thomas2024clarity} & 0.441 & -- \\
\quad RoBERTa-base (FT) \citep{thomas2024clarity} & 0.530 & -- \\
\quad XLNet-base (FT) \citep{thomas2024clarity} & 0.518 & -- \\
\midrule
\multicolumn{3}{l}{\textit{Development baselines (OOF)}} \\
\quad Majority class & 0.248 & 0.052 \\
\quad TF-IDF + LR & 0.546 & 0.319 \\
\quad v3-large (single model) & $.643{\pm}.024$ & $.327{\pm}.040$ \\
\quad xlarge (single model) & $.663{\pm}.021$ & -- \\
\midrule
\multicolumn{3}{l}{\textit{Our ensembles (OOF)}} \\
\quad v3-large (50 models) & 0.667 & 0.373 \\
\quad xlarge (50 models) & 0.679 & -- \\
\midrule
\multicolumn{3}{l}{\textit{Official evaluation}} \\
\quad Single-seed v3-large & 0.54 & 0.50 \\
\quad \textbf{Multi-seed ensemble} & \textbf{0.76} {\tiny(18/41)} & \textbf{0.50} {\tiny(12/33)} \\
\quad + post-hoc optimizations & 0.74 & 0.40 \\
\bottomrule
\end{tabular}}
\caption{Development and official results. $^\dagger$Prior work baselines were evaluated on the original QEvasion test split (317 samples) \citep{thomas2024clarity}, not the SemEval evaluation set (237 samples). \citet{thomas2024clarity} do not report a plain BERT baseline; the closest encoder baselines are DeBERTa-base, RoBERTa-base, and XLNet-base, with ChatGPT (gpt-3.5-turbo) as the reported LLM baseline. T1 uses direct clarity macro F1; T2 uses 9-class evasion macro F1 (reported only for ChatGPT). ``Single model'' = mean$\pm$std over held-out-fold F1 for all 50 models. Rank in parentheses.}
\label{tab:results}
\end{table}

\subsection{Ensemble Size Ablation}

Figure~\ref{fig:ablation} shows OOF macro F1 as a function of ensemble size, computed by incrementally adding models and re-evaluating on the OOF predictions. For DeBERTa-xlarge on Subtask~1, the xlarge single-model mean is 0.663~$\pm$~0.021 (Table~\ref{tab:results}), and the 50-model ensemble achieves 0.679; the curve starts at 0.323 for a single fold~$\times$~single seed, which reflects extreme per-fold evaluation variance on $\sim$690 samples rather than typical single-model performance. The steepest gains occur in the first 10--15 models, with diminishing returns thereafter, confirming that the ensemble consistently outperforms typical individual runs.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig2_ensemble_size_ablation.png}
\caption{OOF macro F1 vs.\ ensemble size for both architectures and subtasks. For DeBERTa-v3-large, most gains are achieved within the first 25 models; DeBERTa-xlarge benefits more gradually across the full ensemble, with the largest late-stage gain between 40 and 50 models.}
\label{fig:ablation}
\end{figure*}

\subsection{The Optimization Paradox}
\label{sec:paradox}

A central finding is that \emph{every} post-hoc optimization we attempted improved OOF performance but degraded evaluation performance. Table~\ref{tab:paradox} summarizes this pattern across two categories of intervention, each measured against its appropriate baseline. We view the result as suggestive evidence rather than a universal claim, because the evaluation set is small and therefore noisy.

\begin{table}[t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}llcc@{}}
\toprule
\textbf{Intervention} & \textbf{Task} & \textbf{OOF $\Delta$} & \textbf{Eval $\Delta$} \\
\midrule
\multicolumn{4}{l}{\textit{Model-level ($\Delta$ vs.\ single-model mean)}} \\
\quad Multi-seed ens.\ & T1 & +0.016 & \textbf{+0.22} \\
\quad Multi-seed ens.\ & T2 & +0.046 & 0.00 \\
\midrule
\multicolumn{4}{l}{\textit{Prediction-level ($\Delta$ vs.\ multi-seed ensemble)}} \\
\quad Optuna weights \citep{akiba2019optuna} & T1 & +0.003 & $-$0.02 \\
\quad Class thresholds & T1 & +0.007 & $-$0.02 \\
\quad Hier.\ masking & T2 & +0.026 & $-$0.10 \\
\bottomrule
\end{tabular}}
\caption{The optimization paradox. OOF~$\Delta$ vs.\ single-model mean (xlarge for T1, v3-large for T2); Eval~$\Delta$ vs.\ our single-seed v3-large submission (the only pre-ensemble submission for either subtask). The +0.22 T1 entry therefore conflates architecture (v3-large~$\rightarrow$~xlarge) and ensembling effects and is an upper bound on the pure ensembling contribution; prediction-level rows all use the same ensemble backbone and cleanly isolate calibration effects.}
\label{tab:paradox}
\end{table}

The most striking case is \textbf{hierarchical masking}, which learns a 3$\times$9 matrix $\mathbf{M}$ to adjust Subtask~2 logits based on Subtask~1 probabilities: $\mathbf{z}_2' {=} \mathbf{z}_2 {+} \mathbf{p}_1 \mathbf{M}$. On OOF data, this improved F1 by +0.026, but on evaluation it caused a $-$0.10 drop, likely because Subtask~1 errors propagated through $\mathbf{M}$ and amplified Subtask~2 errors.

We identify a key distinction: the multi-seed ensemble is a \textit{model-level} intervention (training different models), while all failed optimizations are \textit{prediction-level} interventions (transforming fixed OOF outputs). The former introduces genuine diversity; the latter can only redistribute decisions within a fixed prediction space, making it more prone to overfitting when the OOF distribution differs from evaluation.

With only 237 evaluation samples, the 95\% CI for macro F1 is approximately $\pm$0.06: the $-$0.02 T1 drops individually fall within noise, while the $-$0.10 T2 drop from hierarchical masking clearly exceeds it. The paradox should therefore be interpreted cautiously. Our main evidence is the consistency of the directional pattern---\emph{every} prediction-level intervention degraded evaluation while model-level ensembling improved it---rather than any claim that each individual drop is statistically decisive. A plausible interpretation is that prediction-level tuning overfits to distributional artifacts in OOF predictions (e.g., class-specific calibration biases) that do not transfer when evaluation class proportions differ, whereas model-level diversity is more robust to such shifts.

\subsection{Error Analysis and Task Insights}
\label{sec:errors}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig4_confusion_matrices.pdf}
\caption{OOF confusion matrices (normalized by true label). Left: Subtask~1 (DeBERTa-xlarge, 50 models), showing strong bidirectional confusion between \textit{Ambivalent} and \textit{Clear Reply}. Right: Subtask~2 (DeBERTa-v3-large, 50 models), showing systematic minority-class collapse into \textit{Explicit}.}
\label{fig:confusion}
\end{figure*}

\paragraph{Subtask~1: The Ambivalent Boundary Problem.}
As shown in Figure~\ref{fig:confusion} (left), the primary difficulty in Subtask~1 is distinguishing \textit{Ambivalent} from \textit{Clear Reply}: these two classes account for 734 of 986 total OOF errors (74.4\%). This confusion reflects a genuine annotation challenge since many political responses begin with a superficially direct statement but embed hedging or topic shifts. For example, the response \textit{``No. I think we're going to be strategic partners''} to a question about U.S.-China relations was annotated as \textit{Ambivalent} (the initial ``No'' contradicts the subsequent positive framing), but our model predicts \textit{Clear Reply} with 0.605 confidence, likely due to the unambiguous ``No'' token. This suggests that detecting \textit{pragmatic} evasion (where surface-level directness masks substantive non-answers) remains a core challenge.

\paragraph{Subtask~2: Majority-Class Collapse.}

Figure~\ref{fig:confusion} and Table~\ref{tab:per_class} (Appendix~\ref{sec:per_class}) reveal that Subtask~2 errors follow a systematic pattern: the model misclassifies minority evasion types as the dominant \textit{Explicit} class (188 \textit{Implicit}, 139 \textit{General}, 115 \textit{Dodging}, and 110 \textit{Deflection} samples). This ``majority-class collapse'' is most extreme for \textit{Partial/half-answer} (79 training samples, F1=0.00), which is never predicted. The pattern suggests that standard cross-entropy training is insufficient for this extreme imbalance. We implemented focal loss \citep{lin2017focal} ($\gamma{=}2.0$) and inverse-frequency class weighting as alternatives, but did not submit these variants during the evaluation phase: with only three submission slots per subtask and our Subtask~1 ensemble already performing well, we prioritized submission stability over Subtask~2 exploration. Post-acceptance experiments with these loss functions (Appendix~\ref{sec:imbalance}) confirm that inverse-frequency class weighting yields a modest +.024 OOF macro F1 gain over plain cross-entropy, while focal loss did not help.

Interestingly, \textit{Clarification} achieves the highest F1 (0.655) despite being a minority class (92 samples), likely because clarification responses have a distinctive linguistic structure (asking questions back). In contrast, \textit{General} (F1=0.136) and \textit{Deflection} (F1=0.234) are semantically close to \textit{Dodging}, differing primarily in the \textit{intent} behind the evasion rather than its surface form, which is a distinction that may require discourse-level reasoning beyond what token-level classifiers can capture.

% ============================================================
% CONCLUSION
% ============================================================
\section{Conclusion}

We presented a multi-seed DeBERTa ensemble system for SemEval-2026 Task~6 (CLARITY), achieving macro F1 scores of 0.76 and 0.50 on Subtasks~1 and~2 respectively. We also report suggestive evidence for an \emph{optimization paradox}: model-level ensembling improved Subtask~1 and did not harm Subtask~2, whereas three prediction-level post-hoc calibrations each regressed evaluation performance. Because the evaluation set is limited, this observation should be interpreted cautiously and rests mainly on the consistent directional pattern rather than any individually definitive drop. Even with that caveat, it offers a practical shared-task recommendation: when evaluation data is limited, invest in training more diverse models before optimizing fixed predictions. Our error analysis suggests that the main challenges are the \textit{Ambivalent}/\textit{Clear Reply} boundary in Subtask~1 and majority-class collapse in the long-tailed Subtask~2 distribution.

% ============================================================
% LIMITATIONS  (ACL: must appear after Conclusion, before References)
% ============================================================
\section*{Limitations}

Our multi-seed ensemble requires training 100 models ($\sim$50 GPU-hours), though our ablation shows that 10--15 seeds capture most of the benefit. We did not explore data augmentation or multi-task learning between subtasks, and our LLM comparison is limited to a single model (Qwen2.5-32B). Our optimization paradox analysis rests on three submissions per subtask, and the limited 237-sample evaluation size likely contributes variance.

% ============================================================
% ETHICS STATEMENT  (ACL: recommended between Limitations and References)
% ============================================================
\section*{Ethics Statement}

Automated classification of political speech carries both social value and risk. It can support media accountability, debate analysis, and political communication research, but classifier errors, especially the \textit{Ambivalent}/\textit{Clear Reply} confusion we document, can mischaracterize a politician's response and should not be treated as definitive judgments. The model may also inherit biases from training data drawn primarily from U.S.\ political interviews, so outputs should be validated by human reviewers before use in high-stakes settings.

% ============================================================
% ACKNOWLEDGMENTS
% ============================================================
\section*{Acknowledgments}

We thank the CLARITY task organizers and Purdue University Fort Wayne's Gilbreth HPC cluster for data and compute support.

% ============================================================
% BIBLIOGRAPHY
% ============================================================
\raggedbottom
\bibliography{custom}

% ============================================================
% APPENDIX
% ============================================================
\appendix

\section{Reproducibility Details}
\label{sec:appendix}

\subsection{Training Hyperparameters}
\label{sec:hyperparams}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Hyperparameter} & \textbf{Value} \\
\midrule
Learning rate & $2 \times 10^{-5}$ \\
Optimizer & AdamW \\
Weight decay & 0.01 \\
Warmup ratio & 0.10 \\
LR scheduler & Linear \\
Epochs & 3 \\
Batch size & 32 (4 $\times$ 8 grad.\ accum.) \\
Max sequence length & 512 tokens \\
Dropout & 0.1 \\
Loss function & Cross-entropy \\
\bottomrule
\end{tabular}
\caption{Training hyperparameters, identical for all 100 models.}
\label{tab:hyperparams}
\end{table}

\subsection{Software Environment}
\label{sec:environment}

Python 3.10, PyTorch 2.1.0, Transformers 4.36.0 \citep{wolf2020transformers}, scikit-learn 1.3.2 \citep{pedregosa2011scikitlearn}, CUDA 12.1, NVIDIA A100-80GB.

\subsection{Per-Class Results}
\label{sec:per_class}

\begin{table}[H]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Class} & \textbf{P} & \textbf{R} & \textbf{F1} \\
\midrule
\multicolumn{4}{l}{\textit{Subtask 1 (DeBERTa-xlarge, 50 models)}} \\
\quad Clear Reply & .622 & .666 & .643 \\
\quad Ambivalent & .774 & .759 & .767 \\
\quad Clear Non-Reply & .662 & .598 & .628 \\
\midrule
\multicolumn{4}{l}{\textit{Subtask 2 (DeBERTa-v3-large, 50 models)}} \\
\quad Clarification & .724 & .598 & .655 \\
\quad Explicit & .554 & .744 & .635 \\
\quad Dodging & .487 & .534 & .509 \\
\quad Declining to answer & .545 & .462 & .500 \\
\quad Claims ignorance & .603 & .319 & .418 \\
\quad Implicit & .247 & .291 & .267 \\
\quad Deflection & .294 & .194 & .234 \\
\quad General & .221 & .098 & .136 \\
\quad Partial/half-answer & .000 & .000 & .000 \\
\bottomrule
\end{tabular}
\caption{Per-class OOF results (held-out folds, $\sim$3{,}448 predictions). These OOF values differ from the official evaluation scores (0.76/0.50) because each OOF prediction comes from a model trained on only $4/5$ of the data; see \S5.1 for discussion of the OOF-to-eval gap. Subtask~2 minority classes collapse into \textit{Explicit}, with \textit{Partial/half-answer} never predicted.}
\label{tab:per_class}
\end{table}

\subsection{Seed Variance}
\label{sec:seed_variance}

\begin{figure}[H]
\centering
\includegraphics[width=\columnwidth]{fig3_seed_variance.pdf}
\caption{Per-seed OOF macro F1 across all 50 models per architecture, illustrating the fine-tuning instability that motivates multi-seed ensembling.}
\label{fig:seed_variance}
\end{figure}

\subsection{Class Imbalance Mitigation}
\label{sec:imbalance}

To address Subtask~2 class imbalance, we compare three loss functions using DeBERTa-v3-large with 5-fold CV. Table~\ref{tab:loss_ablation} reports per-fold OOF macro F1.

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Loss Function} & \textbf{Macro F1} & \textbf{Min-class F1} \\
\midrule
Cross-entropy (baseline) & .273\,$\pm$\,.034 & .000 \\
Focal loss ($\gamma{=}2.0$) & .260\,$\pm$\,.047 & .000 \\
Class-weighted CE & \textbf{.297\,$\pm$\,.022} & .000 \\
\bottomrule
\end{tabular}
\caption{Loss function ablation on Subtask~2 (OOF macro F1, mean$\pm$std over 5 folds, single seed, DeBERTa-v3-large). Min-class F1 = F1 of the worst-performing class.}
\label{tab:loss_ablation}
\end{table}

Class-weighted CE provides a modest +0.024 absolute macro F1 improvement over plain CE while reducing fold variance (.022 vs.\ .034). Inspecting per-class F1, the gain comes from rare-class recovery: Deflection improves from .045 to .193, General from .027 to .129, and Claims ignorance from .230 to .434, at the cost of a $-$.21 drop on the majority Explicit class (.587$\rightarrow$.379). Focal loss with $\gamma{=}2.0$ underperforms plain CE in our setting, suggesting that down-weighting easy examples does not transfer benefits from object-detection contexts to this 9-class evasion task. Critically, all three losses produce zero F1 on \emph{Partial/half-answer} (21 evaluation samples per fold on average), indicating that ultra-rare classes require either data augmentation or hierarchical decomposition rather than re-weighting alone. These results suggest class weighting is the most promising single-fix mitigation, but full closure of the imbalance gap likely requires methods beyond loss reshaping.

\end{document}
